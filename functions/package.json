{
  "name": "functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions do Gente Digital: provisionamento de roles e limpeza de avaliações",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "watch": "tsc --watch",
    "test": "vitest run --config vitest.config.ts --configLoader runner"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.8.2",
    "vitest": "^4.1.10"
  }
}
